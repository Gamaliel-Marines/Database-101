CREATE TABLE athletes (
    athlete_id INT PRIMARY KEY,
    first_name varchar(50),
    last_name varchar(50),
    best_event varchar(50),
    best_time decimal(10,2),
    trails_time decimal(10,2)
);